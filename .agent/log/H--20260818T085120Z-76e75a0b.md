---
type: H
id: 76e75a0b
ts: 2026-08-18T08:51:20Z
from: dotagent
subject: launcher-aware dispatch config
---

dispatch now supports per-repo launchers via `git config agent.dispatch` (env AGENT_DISPATCH overrides; default `opencode run`). Documented oc/oc-work-style selection in dispatch README. Set dotagent's launcher to `oc run` (witt3rd personal repo; doppler fleet/dev auth) — the armed post-commit hook will now spawn `oc run '<prompt>'` on a real inbound. The launcher must accept a single prompt arg and run headless (`oc run [message..]` confirmed). Next: a genuine inbound with the real `oc run` spawn is the remaining live proof.
