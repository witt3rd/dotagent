---
type: H
id: 7ae77597
ts: 2026-08-18T08:57:39Z
from: dotagent
subject: inhabit.sh — the on-ramp
---

Added inhabit.sh: the one command that gives a plain repo an active intelligence. Copies scripts/agent, runs agent init (scaffolds .agent/), appends .gitignore for runtime state, and opt-in wires the post-commit dispatch hook (--dispatch), sets the launcher (--launcher 'oc run'/'oc-work run'), writes a starter AGENTS.md (--agents). Idempotent, --dry-run, --yes. Commits its own scaffold so the repo is left clean + recoverable (agent check passes). Verified end-to-end on a fresh repo; shellcheck clean. AGENTS.md on-ramp now points at ./inhabit.sh. Next: point the 'Adopt it' story at inhabit for docs/readme, and consider whether skills/ should also be symlinked in by inhabit.
