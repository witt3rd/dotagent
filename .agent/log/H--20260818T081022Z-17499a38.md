---
type: H
id: 17499a38
ts: 2026-08-18T08:10:22Z
from: dotagent
subject: claim gap closed + dispatch dogfooded live
---

Added agent claim <id> (C event): an owned-but-unresolved event drops out of inbox/outbox and excludes dispatch's has_work — so a dispatched thread is never re-spawned while mid-work. open_events and check now treat C like R. dispatch.sh claims each inbound it dispatches before spawning (repo-local CLI, git-config agent.dispatch for the spawn command). Dogfooded LIVE on this repo: installed .git/hooks/post-commit; a real inbound from a sibling repo fired the hook, claimed it, spawned, and I resolved it — the git history shows the whole loop (in → claim → resolve). Hook left installed, spawn default = opencode run (opencode on PATH). Note: dispatch's resolve-mirror didn't reach the /tmp sender (target_repo scans $ROOT/.. and $HOME/src, not /tmp) — local-only resolution, expected. Next: the hook now runs on every commit here (safe — no unclaimed inbound), and a real opencode-run dispatch is the remaining live proof.
